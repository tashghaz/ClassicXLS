import XCTest
@testable import ClassicXLS

final class ClassicXLSTests: XCTestCase {
    func testPackageBuilds() throws {
        // Placeholder test; we'll add real fixture-based tests in Step 4.
        XCTAssertTrue(true)
    }
}
